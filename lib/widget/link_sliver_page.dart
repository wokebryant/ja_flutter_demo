import 'package:flutter/cupertino.dart';

class LinkSliverPage extends StatelessWidget {
  const LinkSliverPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
