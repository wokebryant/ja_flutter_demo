import 'package:flutter/cupertino.dart';

class AnimationButtonPage extends StatelessWidget {
  const AnimationButtonPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
