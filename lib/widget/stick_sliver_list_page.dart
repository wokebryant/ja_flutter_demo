import 'package:flutter/cupertino.dart';

class StickSliverListPage extends StatelessWidget {
  const StickSliverListPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
