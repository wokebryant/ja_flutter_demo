import 'package:flutter/cupertino.dart';

class RefreshPage2 extends StatelessWidget {
  const RefreshPage2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
