import 'package:flutter/cupertino.dart';

class TickClickPage extends StatelessWidget {
  const TickClickPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
