import 'package:flutter/cupertino.dart';

class SliverTabPage extends StatelessWidget {
  const SliverTabPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
