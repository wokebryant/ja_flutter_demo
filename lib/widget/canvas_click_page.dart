import 'package:flutter/cupertino.dart';

class CanvasClickPage extends StatelessWidget {
  const CanvasClickPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
