import 'package:flutter/cupertino.dart';

class ChatListScrollPage2 extends StatelessWidget {
  const ChatListScrollPage2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
