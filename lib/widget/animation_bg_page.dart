import 'package:flutter/cupertino.dart';

class AnimationBgPage extends StatelessWidget {
  const AnimationBgPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
