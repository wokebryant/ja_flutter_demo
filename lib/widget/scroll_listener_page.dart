import 'package:flutter/cupertino.dart';

class ScrollListenerPage extends StatelessWidget {
  const ScrollListenerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
