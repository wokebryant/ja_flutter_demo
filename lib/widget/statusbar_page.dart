import 'package:flutter/cupertino.dart';

class StatusBarPage extends StatelessWidget {
  const StatusBarPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
