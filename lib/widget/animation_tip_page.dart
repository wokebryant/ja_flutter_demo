import 'package:flutter/cupertino.dart';

class AnimationTipPage extends StatelessWidget {
  const AnimationTipPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
