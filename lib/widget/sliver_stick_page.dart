import 'package:flutter/cupertino.dart';

class SliverStickPage extends StatelessWidget {
  const SliverStickPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
