import 'package:flutter/cupertino.dart';

class ScrollHeaderPage extends StatelessWidget {
  const ScrollHeaderPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
