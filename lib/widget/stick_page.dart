import 'package:flutter/cupertino.dart';

class StickPage extends StatelessWidget {
  const StickPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
