import 'package:flutter/cupertino.dart';

class ColorProgressPage extends StatelessWidget {
  const ColorProgressPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
