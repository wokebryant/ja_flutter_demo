import 'package:flutter/cupertino.dart';

class SliderVerifyPage extends StatelessWidget {
  const SliderVerifyPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
