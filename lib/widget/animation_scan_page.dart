import 'package:flutter/cupertino.dart';

class AnimationScanPage extends StatelessWidget {
  const AnimationScanPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
