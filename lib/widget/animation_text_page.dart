import 'package:flutter/cupertino.dart';

class AnimationTextPage extends StatelessWidget {
  const AnimationTextPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
