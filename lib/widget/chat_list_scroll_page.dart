import 'package:flutter/cupertino.dart';

class ChatListScrollPage extends StatelessWidget {
  const ChatListScrollPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
