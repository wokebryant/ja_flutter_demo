import 'package:flutter/cupertino.dart';

class IndexStackDragCardPage extends StatelessWidget {
  const IndexStackDragCardPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
