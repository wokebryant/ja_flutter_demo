import 'package:flutter/cupertino.dart';

class SliverTabPage2 extends StatelessWidget {
  const SliverTabPage2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
