import 'package:flutter/cupertino.dart';

class SliverTabPage3 extends StatelessWidget {
  const SliverTabPage3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
