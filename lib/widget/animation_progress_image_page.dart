import 'package:flutter/cupertino.dart';

class AnimationProgressImagePage extends StatelessWidget {
  const AnimationProgressImagePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
