import 'package:flutter/cupertino.dart';

class DropSelectPage extends StatelessWidget {
  const DropSelectPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
