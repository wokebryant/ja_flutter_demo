import 'package:flutter/cupertino.dart';

class MatrixCustomPainterPage extends StatelessWidget {
  const MatrixCustomPainterPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
