import 'package:flutter/cupertino.dart';

class ScrollToIndexPage extends StatelessWidget {
  const ScrollToIndexPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
