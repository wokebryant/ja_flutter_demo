import 'package:flutter/cupertino.dart';

class InputBottomPage extends StatelessWidget {
  const InputBottomPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
