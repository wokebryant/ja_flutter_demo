import 'package:flutter/cupertino.dart';

class BottomAnimationNavPage extends StatelessWidget {
  const BottomAnimationNavPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
