import 'package:flutter/cupertino.dart';

class WrapContentPage extends StatelessWidget {
  const WrapContentPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
