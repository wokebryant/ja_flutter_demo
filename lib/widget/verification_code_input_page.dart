import 'package:flutter/cupertino.dart';

class VerificationCodeInputPage extends StatelessWidget {
  const VerificationCodeInputPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
