import 'package:flutter/cupertino.dart';

class RefreshPage3 extends StatelessWidget {
  const RefreshPage3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
